<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link href="https://fonts.googleapis.com/css?family=IBM+Plex+Sans:400,400i,700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/chart.js@2.9.4/dist/Chart.min.js"></script>
    <title>Finance Tracker</title>
</head>

<body class="flex-column min-100-vh">
    <header class="hero">
        {{#if loggedIn}}
        <a href="/dashboard" class="nav-links">Home</a>
        <button id="logout" class="nav-links">Logout</button>
        <a href="/dashboard" class="username">{{username}} &nbsp;<i class="fas fa-user"></i></a>
        <br />
        {{else}}
        <a href="/" class="nav-links">Home</a>
        <a href="/login" class="nav-links">Login</a>
        <br />
        {{/if}}
        <h1 class="app-title"><i class="fas fa-hand-holding-usd"></i> Finance Tracker</h1>
    </header>

    <main class="flex-row">
        {{{ body }}}
    </main>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>